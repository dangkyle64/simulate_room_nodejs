//Logic for interacting with AI models (e.g., loading models, processing images)

const imageTo3DFile = (imagepathorarrayofimages) => {
    //call model preduction function 
    //return results
};

const processRaw3DData = (_3dfileoutput) => {
    //handle the raw 3D data from NeRF or Pix2Vox
    //convert into a file or displayable object
};


